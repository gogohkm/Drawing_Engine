# 도면 그리기 개선방안 01

## 배경
- stgen MCP DXF Viewer를 사용하여 폭 20m, 길이 100m 박공지붕 철골골조 도면 작성
- 횡단면도 + 지붕골조 평면도 + 처마부 접합 상세 수정 작업 수행
- 결과물의 품질이 실무 수준에 미치지 못하는 문제 확인

---

## 1. 품질 저하 원인 분석

### 1.1 좌표 수동 계산의 한계
경사 래프터(10°)의 오프셋 라인을 그리기 위해 모든 좌표를 삼각함수로 직접 계산해야 했음.

```
상부 래프터 오프셋: (-sin10°×200, cos10°×200) = (-34.73, 196.96)
지붕면 오프셋:     (-sin10°×50,  cos10°×50)  = (-8.68, 49.24)
천장선 오프셋:     (sin10°×130, -cos10°×130)  = (22.57, -128.03)
```

- 미세한 오차가 누적됨
- `offset_entity` 같은 CAD 기본 기능이 불안정하여 절대좌표로 모든 것을 계산해야 함
- 수정/변경 시 관련 좌표 전체를 재계산해야 하는 비효율

### 1.2 엔티티 개별 생성 (배치 처리 불가)
- 기둥 42개 배치에 **42번의 개별 API 호출** 필요
- `array_copy`, `create_block/insert_block` 존재하지만, 단순 반복 배치에도 개별 호출이 더 안전
- 실제 CAD: 1개 그리고 Array → 끝
- MCP: 1개씩 좌표 계산 → 개별 호출 × N번

### 1.3 한글 텍스트 렌더링 실패
- 뷰어에서 한글이 `\uc9c0\ubd95\uae30\uc640` 같은 유니코드 이스케이프로 표시
- 주석의 가독성이 크게 저하
- 실제 DXF 파일에는 정상 저장되나 뷰어 내 확인 불가

### 1.4 선 가중치(Line Weight) 제어 불가
실제 도면의 선 구분:
| 선 종류 | 가중치 | 용도 |
|---------|--------|------|
| 굵은 선 | 0.5~0.7mm | 단면 절단선, 외곽선 |
| 중간 선 | 0.25~0.35mm | 보이는 모선 |
| 가는 선 | 0.13~0.18mm | 치수, 주석, 해치 |

현재 모든 선이 동일 두께 → 도면이 밋밋하고 가독성 저하

### 1.5 해치/패턴 표현 부재
- 단열재 지그재그 패턴 표현 불가
- 콘크리트 해치, 지반 해치 등 재료 표현 제한
- `create_hatch` 존재하나 복잡한 경계 영역 지정 어려움
- 커스텀 패턴(단열재 코일 등)은 미지원

### 1.6 지시선(Leader) 없는 주석
- 참고 이미지: **화살표 지시선 → 텍스트** 구조
- 현재: 텍스트만 배치, 어디를 가리키는지 불명확
- `create_leader` 존재하나 활용이 제한적

### 1.7 표준 심볼/블록 라이브러리 부재
매번 처음부터 그려야 하는 요소들:
- 그리드 버블 (원 안에 번호)
- 단면 마크
- 볼트 심볼 (나사산, 너트, 와셔)
- 용접 기호
- H형강 단면 프로파일

### 1.8 상세 표현의 한계
접합부 상세도 수준을 위해 필요하지만 표현하기 어려운 요소:
- H형강 웹/플랜지 프로파일
- 볼트 단면 상세
- 단열재 패턴
- 재료별 해치
- 모든 것이 **절대좌표 + 기본도형**으로만 가능

---

## 2. 근본 원인 요약

| 구분 | 문제 | 영향 |
|------|------|------|
| **도구** | 엔티티 1개씩 개별 생성 | 비효율, 좌표 오차 누적 |
| **도구** | offset/trim/fillet 불안정 | 수동 계산 강제 |
| **렌더링** | 선 가중치 미지원 | 도면 가독성 저하 |
| **렌더링** | 한글 폰트 미지원 | 주석 깨짐 |
| **표현** | 해치/패턴 제한 | 재료 표현 불가 |
| **표현** | 심볼 라이브러리 없음 | 매번 원시도형으로 작도 |
| **워크플로우** | 모든 좌표가 절대값 | 수정/변경 시 전체 재계산 |

---

## 3. 개선 방향 제안

### 3.1 단기 개선 (현 도구 내)
1. **블록 라이브러리 사전 등록**: 그리드 버블, 볼트 심볼, H형강 단면 등을 블록으로 미리 생성하여 재사용
2. **좌표 계산 자동화**: Python 함수로 경사 오프셋, 교점 등 기하 계산을 사전에 처리
3. **템플릿 DXF 활용**: 레이어, 선 가중치, 치수 스타일을 미리 설정한 템플릿 파일 사용

### 3.2 중기 개선 (하이브리드 방식)
1. **Python 스크립트 병행**: `ezdxf` 라이브러리로 복잡한 좌표 계산과 반복 배치를 코드로 처리 → stgen 뷰어로 확인/수정
2. **매크로/시퀀스 확장**: Drawing Engine의 시퀀스 시스템에 접합부 상세, 표준 프레임 등 고수준 패턴 추가
3. **표준 상세 블록 DB**: 자주 사용하는 접합부 상세를 블록으로 DB화

### 3.3 장기 개선 (도구 자체)
1. **배치 엔티티 생성 API**: 하나의 호출로 다수 엔티티 생성
2. **상대좌표/파라메트릭 지원**: "이 선에서 50mm 오프셋" 같은 상대 연산
3. **선 가중치/스타일 완전 지원**
4. **한글 폰트 렌더링 수정**
5. **구조 도면 전용 심볼 팔레트**

---

## 4. 적합한 활용 범위

### stgen MCP가 적합한 작업
- 전체 배치도, 그리드 계획
- 단순 프레이밍 다이어그램
- 빠른 스케치/검토용 도면
- 기존 도면 수정 (엔티티 이동, 복사, 삭제)

### 별도 도구가 필요한 작업
- 시공 상세도 수준의 접합부 도면
- 재료 표현이 필요한 벽체/지붕 상세
- 선 가중치 구분이 필수인 납품용 도면
- 복잡한 해치/패턴이 포함된 단면도

---

*작성일: 2026-02-24*
*작성 근거: 폭20m×길이100m 박공지붕 철골골조 도면 작성 경험*
